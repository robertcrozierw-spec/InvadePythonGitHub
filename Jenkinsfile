
//We will create a Pipeline
pipeline{
    agent any //we specify the the agent(current Jenkins server)

    stages { //dev, uat etc
        stage ('Dev'){
            steps {
                echo 'Hello World'
            }
        }
    }
}